refactor(lissajous): calculate cycles from frequency

This commit refactors the lissajous app to automatically calculate the
number of cycles required to draw a closed curve based on the frequency
ratio. This simplifies the UI by removing the "Cycles" slider and makes
the animation more mathematically correct.

Gemini commit: can we just calculate cycles from frequency?