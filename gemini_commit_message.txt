feat: Add root handler to list available services

This change adds a new handler for the root URL (/) that displays a list of links to the registered applications, making the server more user-friendly.

Gemini commit: now that apps are registered, let's handle the / url by emitting a list of links to the available services