fix: Add animdemo package to server data dependencies

This change updates the server's BUILD.bazel file to include the newly created animdemo_pkg as a data dependency, ensuring it is available to the server at runtime.

Gemini commit: still need to add it to server/BUILD.bazel